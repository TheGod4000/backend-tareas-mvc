
# Dependencias
node_modules/

# Variables de entorno (nunca commitear secretos)
.env
.env.local
.env.*.local

# Certificados TLS autofirmados (no versionar claves privadas)
certs/key.pem
certs/*.key

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
